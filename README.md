# Break and Continue

This project demonstrates the break and continue statements in C#. The active example uses continue to skip even numbers and print only odd numbers. The commented-out example shows how break exits the loop early.

## What I Learned
- How continue skips the current iteration and moves to the next
- How break exits the loop entirely
- The difference between break and continue
- How to use continue to filter values in a loop
